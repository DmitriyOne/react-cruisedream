/* eslint-disable no-unused-vars */
export enum EDirection {
	row = 'row',
	column = 'column'
}

export enum EAlign {
	start = 'start',
	center = 'center',
	end = 'end'
}

export enum EJustify {
	start = 'start',
	end = 'end',
	around = 'around',
	between = 'between',
	center = 'center',
	baseline = 'baseline'
}

export enum ESize {
	full = 'full',
	container = 'container',
	containerXl = 'container-xl'
}
